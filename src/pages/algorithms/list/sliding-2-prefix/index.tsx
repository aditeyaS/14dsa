import React from "react";
import { PageContainer } from "../../../../components";
import Intro from "./intro";

const Sliding2Prefix: React.FC = () => {
  return (
    <PageContainer title="Sliding window, 2 pointer, Prefix sum">
      <Intro />
    </PageContainer>
  );
};

export default Sliding2Prefix;
