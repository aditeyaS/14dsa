import React from "react";
import { PageContainer } from "../../components";
import { SortingIcon } from "../../icons";

export const Sorting: React.FC = () => {
  return (
    <PageContainer title="Sorting" icon={<SortingIcon />}>
      BM
    </PageContainer>
  );
};
